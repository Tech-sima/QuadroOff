# 🚀 Руководство по деплою на различные платформы

## 1. Render.com (Рекомендуется)

### Преимущества:
- ✅ Бесплатный план
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификаты
- ✅ Переменные окружения
- ✅ Поддержка SQLite

### Шаги деплоя:

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте новый **Web Service**
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment**: `Node`
   - **Node Version**: `18` или `20`
5. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `ADMIN_TELEGRAM_ID`
   - `PORT` (Render автоматически установит, но можно указать явно)
   - Остальные из `.env`
6. Нажмите **Create Web Service**

### Важно:
- Render автоматически предоставляет порт через переменную `PORT`
- Приложение "засыпает" после 15 минут бездействия, но Telegram бот будет его "будить"

---

## 2. Railway

### Преимущества:
- ✅ $5 бесплатных кредитов в месяц
- ✅ Очень простой деплой
- ✅ Автоматический HTTPS

### Шаги деплоя:

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект → **Deploy from GitHub repo**
3. Railway автоматически определит Node.js
4. Добавьте переменные окружения в разделе **Variables**
5. Деплой произойдет автоматически

### Важно:
- Нужна карта для бесплатных кредитов (не списывается, если не превышаете лимит)
- Railway автоматически устанавливает `PORT`

---

## 3. Fly.io

### Преимущества:
- ✅ Бесплатный план
- ✅ Глобальная сеть
- ✅ Docker-based

### Шаги деплоя:

1. Установите Fly CLI: `npm install -g @fly/cli`
2. Зарегистрируйтесь: `fly auth signup`
3. В корне проекта создайте `fly.toml` (Fly CLI может создать автоматически)
4. Деплой: `fly deploy`

### Создание fly.toml:

```toml
app = "your-app-name"
primary_region = "fra"

[build]

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true

[[services]]
  protocol = "tcp"
  internal_port = 8000
```

---

## 4. Cyclic.sh

### Преимущества:
- ✅ Специализируется на Node.js
- ✅ Автоматический деплой
- ✅ База данных включена

### Шаги деплоя:

1. Зарегистрируйтесь на [cyclic.sh](https://cyclic.sh)
2. Подключите GitHub репозиторий
3. Cyclic автоматически определит приложение
4. Добавьте переменные окружения
5. Деплой произойдет автоматически

---

## 5. DigitalOcean App Platform

### Преимущества:
- ✅ $200 бесплатных кредитов на 60 дней
- ✅ Надежная инфраструктура
- ✅ Хорошая документация

### Шаги деплоя:

1. Зарегистрируйтесь на [digitalocean.com](https://digitalocean.com)
2. Создайте App Platform → **GitHub**
3. Выберите репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Run Command**: `npm start`
5. Добавьте переменные окружения
6. Деплой

---

## Общие рекомендации для всех платформ:

### Переменные окружения (обязательные):
```env
TELEGRAM_BOT_TOKEN=your_token
ADMIN_TELEGRAM_ID=your_id
PORT=8000  # или оставьте пустым, если платформа устанавливает автоматически
CHANNEL_USERNAME=your_channel
APPROVED_CHAT_LINK=your_link
DATABASE_PATH=./database.sqlite
```

### Опциональные (Google Sheets):
```env
GOOGLE_SPREADSHEET_ID=your_id
GOOGLE_CREDENTIALS={"type":"service_account",...}  # JSON в одну строку
```

### Проверка после деплоя:

1. Проверьте логи на наличие ошибок
2. Убедитесь, что бот отвечает на `/start`
3. Проверьте админ панель по URL, который предоставила платформа
4. Проверьте endpoint `/health` (если доступен)

---

## Сравнительная таблица:

| Платформа | Бесплатный план | Простота | Подходит для бота |
|-----------|----------------|----------|-------------------|
| **Render.com** | ✅ Да | ⭐⭐⭐⭐⭐ | ✅ Отлично |
| **Railway** | ✅ $5 кредитов | ⭐⭐⭐⭐⭐ | ✅ Отлично |
| **Fly.io** | ✅ Да | ⭐⭐⭐ | ✅ Хорошо |
| **Cyclic.sh** | ✅ Да | ⭐⭐⭐⭐ | ✅ Отлично |
| **DigitalOcean** | ✅ Пробный | ⭐⭐⭐⭐ | ✅ Хорошо |
| **Koyeb** | ✅ Пробный | ⭐⭐⭐⭐ | ✅ Хорошо |

---

## Рекомендация:

**Начните с Render.com** - это самый простой и надежный вариант для Telegram ботов с бесплатным планом.

