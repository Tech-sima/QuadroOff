# 🚀 Пошаговая инструкция: Деплой на Northflank

## Шаг 1: Регистрация

1. Перейдите на [northflank.com](https://northflank.com)
2. Нажмите **Sign Up** (или **Get Started**)
3. Зарегистрируйтесь через:
   - GitHub (рекомендуется - проще подключить репозиторий)
   - Email
4. Подтвердите email (если регистрация через email)

---

## Шаг 2: Создание проекта

1. После входа в панель нажмите **Create Project**
2. Введите название проекта (например: `quadrooff-bot`)
3. Выберите регион (например: `Europe (Frankfurt)`)
4. Нажмите **Create Project**

---

## Шаг 3: Подключение репозитория GitHub

1. В созданном проекте нажмите **Add Service**
2. Выберите **GitHub** как источник
3. Если GitHub не подключен:
   - Нажмите **Connect GitHub**
   - Авторизуйте Northflank в GitHub
   - Выберите репозиторий `Tech-sima/quadrooff` (или ваш репозиторий)
4. Выберите:
   - **Repository**: `Tech-sima/quadrooff` (ваш репозиторий)
   - **Branch**: `main` (или `master`)
   - **Build Type**: `Buildpack` (или `Dockerfile`, если создадите)

---

## Шаг 4: Настройка сервиса

### Основные настройки:

1. **Service Name**: `quadrooff-bot` (или любое имя)
2. **Service Type**: `Web Service` (важно для HTTP endpoints)
3. **Buildpack**: 
   - Выберите `Node.js` или оставьте автоматическое определение
   - Northflank автоматически определит Node.js из `package.json`

### Build Settings:

1. **Build Command**: оставьте пустым (Northflank выполнит `npm install` автоматически)
   - Или укажите явно: `npm install`
2. **Start Command**: `npm start`
   - Или: `node src/index.js`

### Runtime Settings:

1. **Node Version**: выберите `18` или `20` (рекомендуется `20`)
2. **Port**: `8000` (или оставьте пустым, Northflank установит автоматически)
3. **Health Check**:
   - **Path**: `/health`
   - **Interval**: `30` секунд
   - **Timeout**: `10` секунд

---

## Шаг 5: Добавление переменных окружения

В разделе **Environment Variables** добавьте все необходимые переменные:

### Обязательные переменные:

1. **TELEGRAM_BOT_TOKEN**
   - Value: ваш токен бота от BotFather

2. **ADMIN_TELEGRAM_ID**
   - Value: ваш Telegram ID

3. **PORT**
   - Value: `8000` (или оставьте пустым, если Northflank устанавливает автоматически)

4. **CHANNEL_USERNAME**
   - Value: `W3Belarus` (или ваш канал)

5. **APPROVED_CHAT_LINK**
   - Value: `https://t.me/+M5bQ2c3VKUgyYzVh` (ваша ссылка на чат)

6. **APPROVED_CHAT_LINK_EN**
   - Value: `https://t.me/+RNs4JlVHQ5lhMTVh` (английская ссылка)

7. **DATABASE_PATH**
   - Value: `./database.sqlite`

### Опциональные (Google Sheets):

8. **GOOGLE_SPREADSHEET_ID**
   - Value: ваш ID таблицы

9. **GOOGLE_CREDENTIALS**
   - Value: JSON credentials в одну строку (без переносов)
   - Пример: `{"type":"service_account","project_id":"...","private_key":"..."}`

### Дополнительные:

10. **NODE_ENV**
    - Value: `production`

---

## Шаг 6: Настройка ресурсов (опционально)

1. **Instance Type**: 
   - Для бесплатного плана: `Free` (256 MB RAM)
   - Если доступно: `Starter` (512 MB RAM)

2. **Scaling**:
   - **Min Instances**: `1`
   - **Max Instances**: `1` (для бота достаточно одного)

---

## Шаг 7: Настройка домена (опционально)

1. В разделе **Domains** можно добавить кастомный домен
2. Или используйте автоматический домен от Northflank (например: `quadrooff-bot-xxx.northflank.app`)

---

## Шаг 8: Деплой

1. Проверьте все настройки
2. Нажмите **Create Service** или **Deploy**
3. Northflank начнет:
   - Клонирование репозитория
   - Установку зависимостей (`npm install`)
   - Сборку приложения
   - Запуск сервиса

---

## Шаг 9: Мониторинг деплоя

1. Перейдите в раздел **Logs**
2. Следите за процессом сборки:
   - ✅ `Installing dependencies...`
   - ✅ `Build completed successfully`
   - ✅ `Starting application...`
3. Проверьте логи runtime на наличие ошибок

---

## Шаг 10: Проверка работоспособности

### Проверка логов:

1. Откройте **Logs** в панели Northflank
2. Убедитесь, что видите:
   ```
   🚀 Приложение запущено успешно!
   📱 Telegram бот активен
   🌐 Админ панель запущена на http://0.0.0.0:8000
   ```

### Проверка бота:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

### Проверка админ панели:

1. Откройте URL сервиса (например: `https://quadrooff-bot-xxx.northflank.app`)
2. Должна открыться админ панель
3. Проверьте endpoint: `https://your-url.northflank.app/health`
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

---

## Возможные проблемы и решения

### Проблема 1: Build failed

**Решение:**
- Проверьте логи сборки
- Убедитесь, что `package.json` корректен
- Проверьте, что все зависимости указаны

### Проблема 2: Service не запускается

**Решение:**
- Проверьте переменные окружения (особенно `TELEGRAM_BOT_TOKEN`)
- Убедитесь, что `PORT` установлен или приложение использует `process.env.PORT`
- Проверьте логи runtime

### Проблема 3: Health check failed

**Решение:**
- Убедитесь, что endpoint `/health` доступен
- Проверьте, что сервер слушает на `0.0.0.0`, а не `localhost`
- Убедитесь, что порт правильный

### Проблема 4: Google Sheets ошибки

**Решение:**
- Проверьте формат `GOOGLE_CREDENTIALS` (должен быть JSON в одну строку)
- Убедитесь, что сервисный аккаунт добавлен в таблицу Google Sheets
- Это не критично - бот будет работать без Google Sheets

---

## Обновление приложения

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Northflank автоматически обнаружит изменения и перезапустит сервис
4. Или нажмите **Redeploy** в панели Northflank

---

## Полезные ссылки

- [Northflank Documentation](https://docs.northflank.com)
- [Northflank Dashboard](https://app.northflank.com)
- [Node.js Buildpack Docs](https://docs.northflank.com/buildpacks/nodejs)

---

## Чеклист перед деплоем

- [ ] Репозиторий загружен на GitHub
- [ ] Все переменные окружения добавлены
- [ ] `PORT` установлен или приложение использует `process.env.PORT`
- [ ] Сервер слушает на `0.0.0.0` (уже исправлено в коде)
- [ ] Endpoint `/health` доступен (уже добавлен)
- [ ] `package.json` содержит скрипт `start`
- [ ] Telegram бот токен валидный
- [ ] Google Sheets credentials корректны (если используется)

---

## Готово! 🎉

После успешного деплоя ваш Telegram бот будет работать 24/7 на Northflank!

